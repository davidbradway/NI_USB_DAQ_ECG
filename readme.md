# README

Setup
```bash
cd ecg_acq
python -m venv venv
```

Operation

- Use Lead II on monitor
- Make sure connections are clean and tight

Usage
```bash
conda activate ecg_acq
venv\Scripts\activate
pip install -r requirementx.txt
python DAQmxGUI.py
```
